import { useState } from "react";
import type { StatistikComponentProps } from ".";

const Print = ({ slug, statistikData }: StatistikComponentProps) => {
	return <>Enter</>;
};

export default Print;
